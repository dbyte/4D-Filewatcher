//%attributes = {}
/* PM: launch
----------------------------------------------------------------
A worker process launcher which spawns a thread for a 4D.SystemWorker.
So at the end we have 2 running workers
---------------------------------------------------------------- */

cs:C1710.ObserverViewController.new().openView()
